# CZ4031

#Assumption
##Your database name is "TPC-H"
##Your database username is "postgres"
##Your database host IP Address is "127.0.0.1"
##Your database name is "5432"


Steps to run the project:
1. Open terminal at code directory
2. pip install psycopg2
3. pip install pyqt6
4. install graphviz-8 https://graphviz.org/download/ from this website +pip install graphviz 
5. Input your password
6. Type 2 query in the two textbox on the top
7. Run the generate button on the left to see the query plan of the left query
8. Run the generate button on the right to see the query plan of the right query + differences and explanation of both